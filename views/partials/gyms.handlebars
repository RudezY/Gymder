<section class="card">
  <a href="/api/gym/{{id}}">
<div class ='gym'>
    <h1>{{gym_name}}</h1>
    available in
    {{#each zipcode as |zip|}}
      <span>{{zip}}</span>
    {{/each}}
    {{!-- <h2>{{zipcode}}</h2> --}}
</div>
  </a>
</section>